# Generate with: openssl rand -hex 32
AUTHENTIK_SECRET_KEY=change-me-generate-with-openssl-rand-hex-32
AUTHENTIK_DB_PASSWORD=change-me

# Optional SMTP (for password reset, invitations)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@fuzzypeanut.local
